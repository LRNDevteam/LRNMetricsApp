Cove Collection Report + Insights — LIVE pack (on top of ReportWorker-PayorXPanel)
================================================================================
Date: 20260915-1856
Lab DB: CoveLRN  (Collection SQL is Cove-only)
Assumes LIVE already has: ReportWorker-PayorXPanel-20260915-1631.zip

What this pack adds
-------------------
COLLECTION (Cove)
  1) Weekly Claim Volume — Wed–Tue weeks anchored on FirstBilledDate (aligns with Production)
  2) Insurance vs Aging — ClaimStatus='No Response'; AgingDOS; Count(ClaimID) NOT DISTINCT
  3) Panel vs Payment — month columns kept (formatted sheet / ClientLogic refresh)
  4) Panel Averages — CheckDate last 6 months; Top 3 payers; flag NOT blank; Count(ClaimID); 30/60 amounts

INSIGHTS (all labs / all reports — Production, Collection, LIS, Executive Summary)
  - Save payload fix (empty dates no longer break JSON binding)
  - WeekRangeStart/End NOT NULL — defaults from week text or today
  - Insights Excel: freeze rows removed; Collection Excel: native PivotTables off (flat grids)

REPORTWORKER Excel (included)
  - Collection / Production exports from current build (no pivots; Insights unfrozen)

SQL changes
-----------
  YES — Sql\FIX_Cove_CollectionSummary_ClientLogic.sql  (CoveLRN only)
  NO new Insights SQL (app-only)

Contents
--------
  Sql\FIX_Cove_CollectionSummary_ClientLogic.sql
  Website\                  IIS LabMetricsDashboard overlay (+ wwwroot\js insights)
  ReportWorker\             LRN - Report Generation Worker overlay
  ClaimLineCSVDataCapture\  nightly refresh overlay (ClientLogic SP names)

ORDER ON THE VM
---------------
A) SQL first (CoveLRN)
B) Website overlay
C) ReportWorker overlay
D) ClaimLineCSVDataCapture overlay

--------------------------------------------------------------------
A) SQL — run against CoveLRN
--------------------------------------------------------------------
  sqlcmd -S <SQLHOST> -d CoveLRN -E -C -f 65001 -b -i Sql\FIX_Cove_CollectionSummary_ClientLogic.sql

  Then refresh once (or wait for next ClaimLineCSVDataCapture run):

  sqlcmd -S <SQLHOST> -d CoveLRN -E -C -Q "EXEC dbo.usp_RefreshCove_CS_InsuranceVsAging_ClientLogic; EXEC dbo.usp_RefreshCove_CS_PanelAverages_ClientLogic; EXEC dbo.usp_RefreshCove_CS_PanelVsPayment_ClientLogic; EXEC dbo.usp_RefreshCove_CS_WeeklyClaimVolume_ClientLogic;"

--------------------------------------------------------------------
B) Website (IIS LabMetricsDashboard)
--------------------------------------------------------------------
  1. Optional: drop app_offline.htm into site root.
  2. Copy Website\* over the live site folder.
     Keep existing appsettings*.json / web.config — do NOT overwrite secrets.
  3. Remove app_offline.htm.
  4. Recycle the app pool.
  5. Hard-refresh browser (Ctrl+F5) before testing Insights Save.

--------------------------------------------------------------------
C) ReportWorker Windows Service
--------------------------------------------------------------------
  1. sc stop "LRN - Report Generation Worker"
  2. Copy ReportWorker\* over the live service folder.
     Keep existing appsettings.json.
  3. sc start "LRN - Report Generation Worker"

--------------------------------------------------------------------
D) ClaimLineCSVDataCapture
--------------------------------------------------------------------
  1. Stop the capture job/service if running.
  2. Copy ClaimLineCSVDataCapture\* over the live folder.
     Keep existing appsettings.json / lab JSON.
  3. Restart the capture job/service.

VERIFY
------
  - Cove Collection UI: week ranges match Production; Aging = No Response + AgingDOS;
    Panel vs Payment months; Panel Averages Top 3 / 30-60.
  - Insights Save on Production / Collection / LIS / Executive Summary (no Missing payload /
    WeekRangeStart NULL errors).
  - Queue Cove Collection Excel: flat sheets (no PivotTables); Insights sheet not frozen.

DO NOT run Northwest FIX_* scripts from this change set.
